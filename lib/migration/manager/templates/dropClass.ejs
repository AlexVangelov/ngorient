  return db.class.update({
    name: '<%= object.name %>',
    superClass: '-ORestricted' // https://github.com/orientechnologies/orientdb/issues/3193
  }).then(function(object) {
    return db.class.drop('<%= object.name %>');
  });