  return db.class.create('<%= object.name %>'<% if (object.superClass) { %>, '<%= object.superClass %>'<% } %>).then(function(object){
    return object.property.create([<% for(var p in params) {%>
      { name: '<%= p %>', type: '<%= params[p] %>' },<% } %>
    ]);
  });